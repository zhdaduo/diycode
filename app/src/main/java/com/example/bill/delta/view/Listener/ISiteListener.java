package com.example.bill.delta.view.Listener;

import android.view.View;

/**
 * Listener Interface for
 * {@link com.example.bill.delta.view.adapter.site.SitesNameViewProvider}.
 */

public interface ISiteListener {

  void SiteListener(View view, String url);
}
